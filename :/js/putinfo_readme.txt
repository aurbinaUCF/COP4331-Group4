putinfo.js README:

Some of the functions of Putinfo.js

The following place the information of the current user into the element in which has these classes:
	.user-name
	.putname
	.putemail
	.putcompany
	.puttoken
	.putprofilepic //puts profile picture into img element
	.projectname //gets the current project selected

.ismanager 
	//for all elements that have this class they will only show if the user is a manager



